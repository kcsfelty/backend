defmodule ListingAppWeb.Resolvers.AgentResolverTest do
  use ListingApp.DataCase

  describe "all_agents/3" do
    test "returns all agents" do

    end
  end

  describe "get_agent/3" do
    test "gets a specific agent" do

    end
    test "returns null for no matches" do

    end
  end

  describe "create_agent/3" do
    test "creates a user" do

    end
  end
end
